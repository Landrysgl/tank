<launch>
    <!-- <let name="urdf_path" 
    value="$(find-pkg-share tank_description/urdf/tank.urdf)"/> -->
   
     <!-- Chemin vers le package -->
  <let name="tank_description_share"
       value="$(find-pkg-share tank_description)" />

  <!-- Chemin complet vers l'URDF -->
  <let name="urdf_path"
       value="$(var tank_description_share)/urdf/tank.urdf" />

  <!-- <let name="rviz_config_file_path" 
       value="$(find-pkg-share tank_bringup/rviz/urdf_config.rviz)"/>
        -->
   
  <let name="tank_bringup_share" value="$(find-pkg-share tank_bringup)" />

  <let name="rviz_config_file_path" value="$(var tank_bringup_share)/rviz/urdf_config.rviz" />


    <node pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="robot_description" value="$(command 'xacro $(var urdf_path)')"/>
    </node>

    <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui"/>

    <node pkg="rviz2" exec="rviz2" output="screen" args="-d $(var rviz_config_file_path)"/>
</launch>